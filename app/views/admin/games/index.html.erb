<p style="color: green"><%= notice %></p>

<h1>Games</h1>


<div id="games">
  <% @games.each do |game| %>
    <p>
      <%= link_to game.name, admin_game_url(game) %>
    </p>
  <% end %>
</div>

<%= link_to "New game", new_admin_game_path %>
